<%= form_for musing do |f| %>
  <%= f.text_field(:title)%>
  <%= text_area_tag(:brain_burp, size: "100x10")%>
  <%= f.submit%>
<% end %>
<!-- 
text_area_tag(:message, "Hi, nice site", size: "24x6") -->